TiPortfolio is a Python Library.


However it provides a CLI for user to use use too.

For example:

`tiportfolio --symbols SPY QQQ GLD --weights 0.5 0.3 0.2 --start 2019-01-01 --end 2024-12-31 --rebalance month_end`